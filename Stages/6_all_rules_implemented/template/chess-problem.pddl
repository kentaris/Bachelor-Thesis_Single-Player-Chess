(define (problem chess-problem)
    (:domain chess)
    (:objects
        ;locations:
;[:locations]
        
        ;object pieces:
;[:object_pieces]
    )
    (:init
        ;initial state s_0:
;[:init_start_state]
;[:init_pawn_start_pos]
;[:init_plusOne]
;[:init_diffByN]
        
        ;last pawn line:
;[:last_pawn_line]
        
        ;castling:
;[:castling]
        
        ;colors:
;[:colors]
        
        ;piece types:
;[:piece_types]
        
        ;turn:
;[:whos_turn]

;[:pawn_promotion_extra_pieces]

        (TRUE)
    )
    (:goal (and
        ;goal state s_*:
            ;board:
;[:goal_position]
            ;removed pieces:
;[:removed]
           )
    )
)